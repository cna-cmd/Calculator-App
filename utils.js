const sum = (x, y) => (+x) + (+y);
const subtraction = (x, y) => +x - +y;
const multiplication = (x, y) => +x * +y
const split = (x, y) => +x / +y
module.exports = {
    sum,
    subtraction,
    multiplication,
    split
}